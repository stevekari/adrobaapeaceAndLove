services:
  - type: web
    name: peace-and-love-adroabaa
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8090
      - key: APP_CORS_ALLOWED_ORIGINS
        value: "*"
